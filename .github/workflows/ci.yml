name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read
  packages: read

jobs:
  lint:
    name: ESLint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run ESLint
        run: npm run lint

  typecheck:
    name: TypeScript
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: npx tsc --noEmit

  vitest:
    name: Vitest Unit Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run vitest
        run: npm test

  cypress-mocked:
    name: Cypress E2E Tests (Mocked)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Run Cypress tests
        uses: cypress-io/github-action@v6
        with:
          build: npm run build
          start: npx serve -s dist -l 3000
          wait-on: "http://localhost:3000"
          wait-on-timeout: 120
          config: video=false
          spec: "cypress/e2e/celina1-mock.cy.ts,cypress/e2e/celina2-mock.cy.ts,cypress/e2e/celina3-mock.cy.ts"

      - name: Upload screenshots on failure
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: cypress-mocked-screenshots
          path: cypress/screenshots
          if-no-files-found: ignore

  cypress-live:
    name: Cypress E2E Tests (Live)
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: banka2
          POSTGRES_USER: banka2user
          POSTGRES_PASSWORD: banka2pass
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U banka2user -d banka2"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=10

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install frontend dependencies
        run: npm ci

      - name: Install PostgreSQL client
        run: sudo apt-get update && sudo apt-get install -y postgresql-client

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Pull backend image
        id: pull-backend
        continue-on-error: true
        run: docker pull ghcr.io/raf-si-2025/banka2-backend:latest

      - name: Skip if backend image unavailable
        if: steps.pull-backend.outcome == 'failure'
        run: |
          echo "::warning::Backend Docker image not available (GHCR private). Skipping Live E2E."
          echo "To fix: make ghcr.io/raf-si-2025/banka2-backend package public, or add a GHCR_PAT secret with read:packages scope."
          echo "SKIP_LIVE=true" >> $GITHUB_ENV

      - name: Start backend container
        if: env.SKIP_LIVE != 'true'
        run: |
          docker run -d --name backend --network host \
            -e SPRING_DATASOURCE_URL="jdbc:postgresql://127.0.0.1:5432/banka2" \
            -e SPRING_DATASOURCE_USERNAME="banka2user" \
            -e SPRING_DATASOURCE_PASSWORD="banka2pass" \
            -e SPRING_DATASOURCE_DRIVER_CLASS_NAME="org.postgresql.Driver" \
            -e SPRING_JPA_HIBERNATE_DDL_AUTO="update" \
            -e SPRING_JPA_PROPERTIES_HIBERNATE_DIALECT="org.hibernate.dialect.PostgreSQLDialect" \
            -e SPRING_JPA_PROPERTIES_HIBERNATE_TYPE_PREFERRED_BOOLEAN_JDBC_TYPE="INTEGER" \
            -e JWT_SECRET="ci-secret-key-for-ci-only-do-not-use-in-prod" \
            -e SPRING_MAIL_HOST="localhost" \
            -e SPRING_MAIL_PORT="25" \
            -e SPRING_MAIL_USERNAME="ci@example.com" \
            -e SPRING_MAIL_PASSWORD="ci-dummy" \
            -e EXCHANGE_API_KEY="ci-dummy-key" \
            -e STOCK_API_KEYS="ci-key-1,ci-key-2,ci-key-3,ci-key-4" \
            ghcr.io/raf-si-2025/banka2-backend:latest

      - name: Wait for backend to be ready
        if: env.SKIP_LIVE != 'true'
        run: |
          echo "Waiting for backend to start..."
          for i in $(seq 1 60); do
            if curl -sf http://localhost:8080/swagger-ui.html > /dev/null 2>&1 || curl -sf http://localhost:8080/ > /dev/null 2>&1; then
              echo "Backend is ready!"
              exit 0
            fi
            echo "Attempt $i - waiting..."
            sleep 5
          done

          echo "Backend failed to start in time."
          docker logs backend || true
          exit 1

      - name: Seed database
        if: env.SKIP_LIVE != 'true'
        env:
          PGPASSWORD: banka2pass
        run: |
          git clone --depth 1 https://github.com/RAF-SI-2025/Banka-2-Backend.git /tmp/backend

          # Sacekaj da Hibernate kreira sve tabele (DDL_AUTO=update ih kreira pri prvom boot-u)
          echo "Cekam da Hibernate kreira sve tabele..."
          TABLE_COUNT=0
          for i in $(seq 1 30); do
            TABLE_COUNT=$(psql -h 127.0.0.1 -U banka2user -d banka2 -t -A -c \
              "SELECT COUNT(*) FROM information_schema.tables WHERE table_schema='public'" 2>/dev/null || echo "0")
            echo "Pokusaj $i: $TABLE_COUNT tabela kreirane"
            if [ "$TABLE_COUNT" -ge 30 ]; then
              echo "Sve tabele kreirane."
              break
            fi
            sleep 2
          done

          if [ "$TABLE_COUNT" -lt 30 ]; then
            echo "::error::Hibernate nije kreirao dovoljno tabela ($TABLE_COUNT < 30)"
            docker logs backend | tail -50
            exit 1
          fi

          # Truncate sve tabele pre seed-a (backend je mozda vec ubacio neke default podatke)
          psql -h 127.0.0.1 -U banka2user -d banka2 -v ON_ERROR_STOP=1 -c \
            "DO \$\$ DECLARE r RECORD; BEGIN
              EXECUTE 'SET session_replication_role = replica';
              FOR r IN SELECT tablename FROM pg_tables WHERE schemaname = 'public' LOOP
                EXECUTE 'TRUNCATE TABLE ' || quote_ident(r.tablename) || ' RESTART IDENTITY CASCADE';
              END LOOP;
              EXECUTE 'SET session_replication_role = DEFAULT';
            END \$\$;"

          # Seed
          psql -h 127.0.0.1 -U banka2user -d banka2 -v ON_ERROR_STOP=1 -f /tmp/backend/seed.sql

          # Verifikuj seed (osnovni smoke checks)
          EMP_COUNT=$(psql -h 127.0.0.1 -U banka2user -d banka2 -t -A -c "SELECT COUNT(*) FROM employees")
          CLIENT_COUNT=$(psql -h 127.0.0.1 -U banka2user -d banka2 -t -A -c "SELECT COUNT(*) FROM clients")
          LISTING_COUNT=$(psql -h 127.0.0.1 -U banka2user -d banka2 -t -A -c "SELECT COUNT(*) FROM listings")
          echo "Seed verifikacija: employees=$EMP_COUNT, clients=$CLIENT_COUNT, listings=$LISTING_COUNT"
          if [ "$EMP_COUNT" -lt 3 ] || [ "$CLIENT_COUNT" -lt 3 ] || [ "$LISTING_COUNT" -lt 5 ]; then
            echo "::error::Seed nije uspesno ubacen podatke"
            exit 1
          fi
          echo "Database seeded successfully!"

          # Restart backend da pokupi seed-ovane podatke (cache refresh)
          docker restart backend
          for i in $(seq 1 30); do
            if curl -sf http://localhost:8080/swagger-ui.html > /dev/null 2>&1 || curl -sf http://localhost:8080/ > /dev/null 2>&1; then
              echo "Backend je restartovan i spreman posle seed-a."
              break
            fi
            sleep 2
          done

      - name: Build frontend
        if: env.SKIP_LIVE != 'true'
        run: npm run build

      - name: Create CI nginx config
        if: env.SKIP_LIVE != 'true'
        run: |
          sed 's|http://banka2_backend:8080/|http://127.0.0.1:8080/|g' nginx.conf > nginx.ci.conf

      - name: Build frontend nginx image
        if: env.SKIP_LIVE != 'true'
        run: docker build -t banka2-frontend:ci-test .

      - name: Start frontend with nginx
        if: env.SKIP_LIVE != 'true'
        run: |
          docker run -d --name frontend --network host \
            -v "$(pwd)/nginx.ci.conf:/etc/nginx/conf.d/default.conf:ro" \
            banka2-frontend:ci-test
          sleep 3

      - name: Run Cypress live tests
        if: env.SKIP_LIVE != 'true'
        uses: cypress-io/github-action@v6
        with:
          install: false
          wait-on: "http://localhost"
          wait-on-timeout: 60
          config: video=false,baseUrl=http://localhost
          spec: "cypress/e2e/celina1-live.cy.ts,cypress/e2e/celina2-live.cy.ts,cypress/e2e/celina3-live.cy.ts,cypress/e2e/e2e-scenario-live.cy.ts"

      - name: Upload screenshots on failure
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: cypress-live-screenshots
          path: cypress/screenshots
          if-no-files-found: ignore

      - name: Show backend logs on failure
        if: failure()
        run: docker logs backend || true

      - name: Show frontend logs on failure
        if: failure()
        run: docker logs frontend || true

  build-docker:
    name: Build Docker Image
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t banka2-frontend:ci-test .